'use client';

import { ReactNode } from 'react';

export default function AIProvider({ children }: { children: ReactNode }) {
  // Placeholder for wiring Vercel AI Components; keeps tree consistent even if no key is provided.
  return <>{children}</>;
}
